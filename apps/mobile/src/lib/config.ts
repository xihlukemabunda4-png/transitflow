// localhost works for the web target (expo start --web) and iOS simulator.
// Android emulator needs 10.0.2.2 instead of localhost to reach the host
// machine — swap this when actually running on an Android emulator/device.
export const API_BASE_URL = 'http://localhost:3001';
